# scripts/

Helper scripts the skill invokes — e.g. shell wrappers around the Things URL
scheme (`open "things:///add?..."`) or `osascript` snippets for reads.

> Placeholder. Add executable scripts here as the skill implementation lands.
> Keep each script focused and document its arguments at the top of the file.
